
services:
  redis:
    image: redis:8
    container_name: async-cache-dedupe-redis
    ports:
      - "6379:6379"
    command: redis-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
